sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t yemretilki/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true yemretilki/react-test npm test

after_success:
  - docker build -t yemretilki/multi-client ./client
  - docker build -t yemretilki/multi-nginx ./nginx
  - docker build -t yemretilki/multi-server ./server
  - docker build -t yemretilki/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push yemretilki/multi-client
  - docker push yemretilki/multi-nginx
  - docker push yemretilki/multi-server
  - docker push yemretilki/multi-worker
